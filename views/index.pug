extends layout

block content
  h1= title
  p Welcome to the XRP EVM faucet. Enter your wallet address below:
  form(action="/", method="POST")
    input(name="client_address", type="text", placeholder="0x0000....")
    button(type="submit") Submit 
